# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile ~/.gitignore_global

# Ignore bundler config
/.bundle

# Ignore foreman config
/.env
/.env.local
/.env.development.local
/.env.test.local
/.env.staging.local
/.env.production.local
!/.env.example
!/.env.development
!/.env.test
!/.env.staging
!/.env.production

# Ignore RubyMine project files
/.idea
*.iml

# Ignore VSCode project files
.vscode/*

# Ignore the default SQLite database.
/db/*.sqlite3

# Ignore database config
# Since Heroku will write a new one on each deploy
/config/database.yml

# Ignore DB Schema Dump, always has conflicts
/db/schema.rb

# Ignore all logfiles and tempfiles.
/log/*.log
/tmp/*
/tmp/cache
/tmp/data
/tmp/pids/*.pid
/tmp/pids/*.pid.lock
/tmp/sitemaps
/tmp/scout_*
/public/uploads/tmp

# Ignore Mac image rubbish
.DS_Store

# Ignore `passenger-standalone.json`(4.x)/`Passengerfile.json`(5.x)
# which is only for development
# All production parameters are put in Procfile
/passenger-standalone.json
/Passengerfile.json

# Ignore vagrant local virtual machines settings & Vagrantfile
/.vagrant
Vagrantfile

# Packaged gems
## Note that this should NOT be put in .dockerignore
## Since this is for running `bundle install`
/vendor/cache

# Ignore all files containing secrets
/ansible/inventories/staging/**/secrets.yml
/ansible/inventories/pre_production/**/secrets.yml
/ansible/inventories/production/**/secrets.yml

# No need to care who got access to vagrant servers
/ansible/inventories/vagrant/**/hosts_authorized_users.yml

# No need to track vagrant & staging releases
/ansible/inventories/staging/**/app_docker_image_tag.yml
/ansible/inventories/pre_production/**/app_docker_image_tag.yml

# No need to track vagrant config
# Since it's hosted on local machine
/ansible/inventories/vagrant/**/inventory_specific_config.yml

# Don't track global secrets
/ansible/roles/common.secrets/defaults/main.yml

# Ignore all runtime compilation of filter plugin
# Since it keeps being updated after each time of running
/ansible/filter_plugins/*.pyc

# Ignore all cert files containing secrets
/ansible/roles/web.nginx/files/secrets/ssl_certs/staging/*.pem
/ansible/roles/web.nginx/files/secrets/ssl_certs/production/*.pem

# Whatsapp secrets
/ansible/roles/spacious.whatsapp_email_bridge/files/secrets

# Vim files
# Ref: https://stackoverflow.com/questions/4824188/git-ignore-vim-temporary-files
*.swp
*.swo

# NPM package
/node_modules
/public/packs
/public/packs-test
# Yarn
/yarn-error.log
yarn-debug.log*
.yarn-integrity
# Yarn 2
# Ref: https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.pnp.*

# Pip local packages
/src/*
